#! /usr/bin/env python
import sys

for line in sys.stdin:
    line = line.strip()
    unpacked = line.split(",")
    user, item, rating  = line.split(",")
    sys.stdout.write('LongValueSum:%s\t1\n' % item)


